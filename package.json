{
  "name": "copo-matrix-webapp",
  "version": "1.0.0",
  "description": "CO-PO Matrix Web Application for CSE Program",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd client && npm install",
    "build": "echo 'Backend does not need build step'",
    "start": "node server/index.js"
  },
  "dependencies": {
    "@mistralai/mistralai": "^1.0.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "docx": "^9.5.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mammoth": "^1.6.0",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
